import styled from 'styled-components';

export const ExchangesLink = styled.span`
  text-decoration: underline;
  font-size: 0.9rem;
  font-weight: 700;
  cursor: pointer;
`;

export const Title = styled.h2`
  font-family: 'Roboto', sans-serif;
  color: #000;
  margin-top: 0;
  font-size: 28px;
`;

export const Subtitle = styled.p`
  font-size: 1rem;
  color: #1a1a1a;
  line-height: 18px;
  margin: 10px 0;
`;

export const TitleSection = styled.h3`
  font-size: 18px;
  color: #1a1a1a;
  margin: 18px 0;
  font-weight: 600;
  letter-spacing: 0;
`;

export const Paragraph = styled.p`
  line-height: 18px;
  margin-bottom: 10px;
  color: #1a1a1a;
`;
